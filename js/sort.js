
//main function to sort and then display vides newest to oldest
function sortAndDisplayVideos(isNewest) {
	const videoCards = [...document.queryselectorAll('.video-card')];
		console.log(videoCards);
}